{
  "name": "play-helpers",
  "version": "0.0.1",
  "private": true,
  "description": "Gameplay development kits and helpers",
  "engines": {
    "pnpm": ">=7.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean",
    "start:server": "node packages/hexagon-editor/server/index.js"
  }
}
